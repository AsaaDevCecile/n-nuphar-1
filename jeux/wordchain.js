// commands/games/wordchain.js (Jeu du mot qui suit)
module.exports = {
    name: 'wordchain',
    description: 'Jeu du mot qui suit',
    async execute(interaction) {
        const usedWords = new Set();
        let lastWord = '';

        // Logique du jeu
    }
};
